#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
	int n, i, total = 0;
	scanf("%d", &n);
	while(n--) {
		scanf("%d", &i);
		total += i;
	}

	printf("%d", i);

	return 0;
}
